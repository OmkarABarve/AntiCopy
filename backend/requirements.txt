# Core API
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
websockets==14.1

# NLP (linguistic + ownership features). Install model with:
#   python -m spacy download en_core_web_sm
spacy==3.8.2

# Numeric helpers
numpy==2.2.1

# --- Optional upgrade paths (not required for the demo) ---
# Uncomment to enable the real streaming ASR / ML upgrade paths.
# scikit-learn==1.6.0
# xgboost==2.1.3
# whisper-live is integrated behind the ENABLE_WHISPERLIVE flag.
